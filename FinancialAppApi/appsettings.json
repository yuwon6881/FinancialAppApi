{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Server=db.wdbdsnpldtiwsmscwyqt.supabase.co;Database=postgres;Port=5432;User Id=postgres;Password=Yuwon_017124;SslMode=Require;Trust Server Certificate=true"
  },
  "WebAuthn": {
    // Exact origin(s) the frontend is served from, e.g. "https://myapp.vercel.app".
    // Leave empty to trust the request's Origin header automatically (fine for
    // getting this working; set explicitly before relying on it for security).
    "AllowedOrigins": []
  }
}
